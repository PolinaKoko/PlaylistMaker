<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"

    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="horizontal"
    android:gravity="center_vertical"
    android:background="?attr/colorSurface"
    android:theme="@style/Theme.PlaylistMaker.TrackItem">

    <ImageView
        android:id="@+id/ivArtwork"
        android:layout_width="@dimen/track_artwork_size"
        android:layout_height="@dimen/track_artwork_size"
        android:layout_marginTop="@dimen/track_artwork_margin_vertical"
        android:layout_marginBottom="@dimen/track_artwork_margin_vertical"
        android:scaleType="centerCrop"
        android:contentDescription="Обложка трека" />

    <LinearLayout
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_weight="1"
        android:layout_marginStart="@dimen/track_text_margin_start"
        android:layout_marginEnd="@dimen/track_text_margin_end"
        android:orientation="vertical">

        <TextView
            android:id="@+id/tvTrackName"
            style="@style/TrackTitleStyle"
            android:layout_marginTop="@dimen/track_title_margin_top"
            tools:text="Smells Like Teen Spirit" />

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginBottom="@dimen/track_subtitle_margin_bottom"
            android:orientation="horizontal"
            android:gravity="center_vertical">

            <TextView
                android:id="@+id/tvArtistName"
                style="@style/TrackSubtitleStyle"
                tools:text="Nirvana" />

            <ImageView
                android:layout_width="@dimen/track_dot_size"
                android:layout_height="@dimen/track_dot_size"
                android:src="@drawable/ic_dot"
                android:layout_gravity="center"
                android:padding="@dimen/track_dot_padding"
                app:tint="?attr/colorOnSurfaceVariant" />

            <TextView
                android:id="@+id/tvTrackTime"
                style="@style/TrackSubtitleStyle"
                tools:text="5:01" />

        </LinearLayout>
    </LinearLayout>

    <ImageView
        android:id="@+id/ivArrowForward"
        android:layout_width="@dimen/icon_size_medium"
        android:layout_height="@dimen/icon_size_medium"
        android:src="@drawable/ic_arrow_forward"
        app:tint="?attr/colorOnSurfaceVariant" />

</LinearLayout>